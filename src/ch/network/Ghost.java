package ch.network;

import java.net.InetSocketAddress;

public class Ghost extends Connection {

    public Ghost(int port, InetSocketAddress receiver) {
        super(port, receiver);
    }

    public void handle
}
